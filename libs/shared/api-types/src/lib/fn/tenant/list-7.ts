/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CustomResponse } from '../../models/custom-response';
import { Pageable } from '../../models/pageable';

export interface List7$Params {

/**
 * Filtre par statut du dossier. Omis = tous les statuts.
 */
  status?: 'INCOMPLETE' | 'PENDING_REVIEW' | 'QUALIFIED' | 'REJECTED' | 'BLACKLISTED';

/**
 * Filtre par bien immobilier (UUID). Omis = tous les biens.
 */
  propertyId?: string;

/**
 * Si `true` et `propertyId` fourni : retourne uniquement les dossiers sans contrat pour ce bien.
 */
  withoutContract?: boolean;
  pageable: Pageable;
}

export function list7(http: HttpClient, rootUrl: string, params: List7$Params, context?: HttpContext): Observable<StrictHttpResponse<CustomResponse>> {
  const rb = new RequestBuilder(rootUrl, list7.PATH, 'get');
  if (params) {
    rb.query('status', params.status, {});
    rb.query('propertyId', params.propertyId, {});
    rb.query('withoutContract', params.withoutContract, {});
    rb.query('pageable', params.pageable, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<CustomResponse>;
    })
  );
}

list7.PATH = '/v1/tenants';
