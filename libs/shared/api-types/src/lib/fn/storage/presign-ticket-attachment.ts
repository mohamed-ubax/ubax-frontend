/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PresignedUrlResponse } from '../../models/presigned-url-response';

export interface PresignTicketAttachment$Params {

/**
 * UUID du ticket
 */
  ticketId: string;

/**
 * Type MIME du fichier
 */
  contentType: string;
  expires?: number;
}

export function presignTicketAttachment(http: HttpClient, rootUrl: string, params: PresignTicketAttachment$Params, context?: HttpContext): Observable<StrictHttpResponse<PresignedUrlResponse>> {
  const rb = new RequestBuilder(rootUrl, presignTicketAttachment.PATH, 'get');
  if (params) {
    rb.query('ticketId', params.ticketId, {});
    rb.query('contentType', params.contentType, {});
    rb.query('expires', params.expires, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PresignedUrlResponse>;
    })
  );
}

presignTicketAttachment.PATH = '/v1/storage/presign/ticket-attachment';
