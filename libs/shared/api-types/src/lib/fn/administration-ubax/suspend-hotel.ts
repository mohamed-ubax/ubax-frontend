/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { AdminHotelResponse } from '../../models/admin-hotel-response';

export interface SuspendHotel$Params {
  id: string;
}

export function suspendHotel(http: HttpClient, rootUrl: string, params: SuspendHotel$Params, context?: HttpContext): Observable<StrictHttpResponse<AdminHotelResponse>> {
  const rb = new RequestBuilder(rootUrl, suspendHotel.PATH, 'patch');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<AdminHotelResponse>;
    })
  );
}

suspendHotel.PATH = '/v1/admin/partners/hotels/{id}/suspend';
