import type { StepConfig } from '../types/contrats-add.types';
import { CONTRACT_STEP_CONFIG } from '../utils/contract-form.utils';

export const CONTRATS_ADD_STEP_CONFIG: readonly StepConfig[] =
  CONTRACT_STEP_CONFIG;
